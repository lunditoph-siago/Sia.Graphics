diff --git a/Cargo.toml b/Cargo.toml
index dde9a8c..6547f1e 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -39,7 +39,7 @@ package = "naga"
 version = "29.0.1"

 [lib]
-crate-type = ["cdylib", "staticlib"]
+crate-type = ["staticlib"]

 [features]
 default = ["wgsl", "spirv", "glsl", "dx12", "metal", "vulkan", "gles"]
diff --git a/build.rs b/build.rs
index 0b5f174..56721d5 100644
--- a/build.rs
+++ b/build.rs
@@ -19,6 +19,7 @@ fn main() {
     println!("cargo:rerun-if-changed=ffi/wgpu.h");
     println!("cargo:rerun-if-env-changed=TARGET");
     println!("cargo:rerun-if-env-changed=BINDGEN_EXTRA_CLANG_ARGS");
+    println!("cargo:rerun-if-env-changed=EMSCRIPTEN_SYSROOT");

     #[rustfmt::skip]
     let types_to_rename = vec![
@@ -89,6 +90,15 @@ fn main() {
             "aarch64-apple-darwin" | "x86_64-apple-darwin" => {
                 builder = builder.clang_arg("-isysroot").clang_arg(sdk_path("macosx"));
             }
+            "wasm32-unknown-emscripten" => {
+                builder = builder
+                    .clang_arg("--target=wasm32-unknown-emscripten")
+                    .clang_arg("--sysroot")
+                    .clang_arg(
+                        env::var("EMSCRIPTEN_SYSROOT")
+                            .expect("EMSCRIPTEN_SYSROOT is required for Emscripten bindgen"),
+                    );
+            }
             _ => {}
         }
     }
diff --git a/ffi/wgpu.h b/ffi/wgpu.h
index 4f7d4e0..e5f8870 100644
--- a/ffi/wgpu.h
+++ b/ffi/wgpu.h
@@ -39,6 +39,8 @@ typedef enum WGPUNativeSType
     WGPUSType_PrimitiveStateExtras = 0x0003000A,
     /** Identifies @ref WGPUSamplerDescriptorExtras. */
     WGPUSType_SamplerDescriptorExtras = 0x0003000B,
+    /** Identifies @ref WGPUEmscriptenSurfaceSourceCanvasHTMLSelector. */
+    WGPUSType_EmscriptenSurfaceSourceCanvasHTMLSelector = 0x00040000,
     WGPUNativeSType_Force32 = 0x7FFFFFFF
 } WGPUNativeSType;

@@ -1365,6 +1367,16 @@ typedef struct WGPUSurfaceSourceSwapChainPanel
     void *panelNative;
 } WGPUSurfaceSourceSwapChainPanel WGPU_STRUCTURE_ATTRIBUTE;

+/**
+ * Chained in @ref WGPUSurfaceDescriptor to select an HTML canvas when
+ * wgpu-native is built for Emscripten.
+ */
+typedef struct WGPUEmscriptenSurfaceSourceCanvasHTMLSelector
+{
+    WGPUChainedStruct chain;
+    WGPUStringView selector;
+} WGPUEmscriptenSurfaceSourceCanvasHTMLSelector WGPU_STRUCTURE_ATTRIBUTE;
+
 typedef enum WGPUPolygonMode
 {
     WGPUPolygonMode_Fill = 0,
diff --git a/src/conv.rs b/src/conv.rs
index f7e51f9..e90d57a 100644
--- a/src/conv.rs
+++ b/src/conv.rs
@@ -1869,7 +1869,18 @@ pub unsafe fn map_surface(
     _metal: Option<&native::WGPUSurfaceSourceMetalLayer>,
     android: Option<&native::WGPUSurfaceSourceAndroidNativeWindow>,
     _swap_chain_panel: Option<&native::WGPUSurfaceSourceSwapChainPanel>,
+    emscripten_canvas: Option<&native::WGPUEmscriptenSurfaceSourceCanvasHTMLSelector>,
 ) -> CreateSurfaceParams {
+    if emscripten_canvas.is_some() {
+        let display_handle = raw_window_handle::WebDisplayHandle::new();
+        let window_handle = raw_window_handle::WebWindowHandle::new(1);
+
+        return CreateSurfaceParams::Raw((
+            raw_window_handle::RawDisplayHandle::Web(display_handle),
+            raw_window_handle::RawWindowHandle::Web(window_handle),
+        ));
+    }
+
     if let Some(win) = win {
         let display_handle = raw_window_handle::WindowsDisplayHandle::new();
         let mut window_handle =
diff --git a/src/lib.rs b/src/lib.rs
index ba92673..96e6ab2 100644
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -445,17 +445,22 @@ const DEFAULT_DEVICE_LOST_HANDLER: DeviceLostCallback = DeviceLostCallback {
 #[derive(Debug)]
 pub enum Error {
     DeviceLost {
-        source: Box<dyn error::Error + Send + 'static>,
+        source: ErrorSource,
     },
     OutOfMemory {
-        source: Box<dyn error::Error + Send + 'static>,
+        source: ErrorSource,
     },
     Validation {
-        source: Box<dyn error::Error + Send + 'static>,
+        source: ErrorSource,
         description: String,
     },
 }

+#[cfg(not(target_family = "wasm"))]
+type ErrorSource = Box<dyn error::Error + Send + 'static>;
+#[cfg(target_family = "wasm")]
+type ErrorSource = Box<dyn error::Error + 'static>;
+
 impl error::Error for Error {
     fn source(&self) -> Option<&(dyn error::Error + 'static)> {
         match self {
@@ -598,6 +603,7 @@ fn format_error(err: &(impl error::Error + 'static)) -> String {
     format!("Validation Error\n\nCaused by:\n{}", output)
 }

+#[cfg(not(target_family = "wasm"))]
 fn handle_error_fatal(
     cause: impl error::Error + Send + Sync + 'static,
     operation: &'static str,
@@ -605,6 +611,12 @@ fn handle_error_fatal(
     panic!("Error in {operation}: {f}", f = format_error(&cause));
 }

+#[cfg(target_family = "wasm")]
+fn handle_error_fatal(cause: impl error::Error + 'static, operation: &'static str) -> ! {
+    panic!("Error in {operation}: {f}", f = format_error(&cause));
+}
+
+#[cfg(not(target_family = "wasm"))]
 fn handle_error(
     sink_mutex: &Mutex<ErrorSinkRaw>,
     source: impl error::Error + Send + Sync + 'static,
@@ -642,6 +654,43 @@ fn handle_error(
     });
 }

+#[cfg(target_family = "wasm")]
+fn handle_error(
+    sink_mutex: &Mutex<ErrorSinkRaw>,
+    source: impl error::Error + 'static,
+    label: Label<'_>,
+    fn_ident: &'static str,
+) {
+    let error = wgc::error::ContextError {
+        fn_ident,
+        source: Box::new(source),
+        label: label.unwrap_or_default().to_string(),
+    };
+    let mut sink = sink_mutex.lock();
+    let mut source_opt: Option<&(dyn error::Error + 'static)> = Some(&error);
+    while let Some(source) = source_opt {
+        match source.downcast_ref::<wgc::device::DeviceError>() {
+            Some(wgc::device::DeviceError::Lost) => {
+                return sink.handle_error(crate::Error::DeviceLost {
+                    source: Box::new(error),
+                });
+            }
+            Some(wgc::device::DeviceError::OutOfMemory) => {
+                return sink.handle_error(crate::Error::OutOfMemory {
+                    source: Box::new(error),
+                });
+            }
+            _ => (),
+        }
+        source_opt = source.source();
+    }
+
+    sink.handle_error(crate::Error::Validation {
+        description: format_error(&error),
+        source: Box::new(error),
+    });
+}
+
 // webgpu.h functions

 #[no_mangle]
@@ -2798,7 +2847,8 @@ pub unsafe extern "C" fn wgpuInstanceCreateSurface(
             WGPUSType_SurfaceSourceWaylandSurface => native::WGPUSurfaceSourceWaylandSurface,
             WGPUSType_SurfaceSourceMetalLayer => native::WGPUSurfaceSourceMetalLayer,
             WGPUSType_SurfaceSourceAndroidNativeWindow => native::WGPUSurfaceSourceAndroidNativeWindow,
-            WGPUSType_SurfaceSourceSwapChainPanel => native::WGPUSurfaceSourceSwapChainPanel)
+            WGPUSType_SurfaceSourceSwapChainPanel => native::WGPUSurfaceSourceSwapChainPanel,
+            WGPUSType_EmscriptenSurfaceSourceCanvasHTMLSelector => native::WGPUEmscriptenSurfaceSourceCanvasHTMLSelector)
     );

     let surface_id = match create_surface_params {
